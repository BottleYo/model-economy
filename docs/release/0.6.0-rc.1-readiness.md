# Model Economy 0.6.0-rc.1 发布就绪记录

> 历史记录：本文保留创建候选标签前的检查快照。`v0.6.0-rc.1` 标签、标签 CI 与 GitHub Pre-release 已于 2026-07-21 完成；后续稳定版状态以 `v0.6.0` Release 为准。

检查时间：2026-07-21（Asia/Shanghai）

结论：候选代码、公开页面、远端 CI 与真实 marketplace 生命周期均已通过，可以创建候选 tag；GitHub Pre-release 仍须等待 tag CI 全绿。

## 已通过

- manifest、`pyproject.toml`、CLI 模板版本与契约测试统一为 `0.6.0-rc.1`。
- 插件 manifest 校验通过，版本同时符合 SemVer；`pyproject` 可按 PEP 440 规范化为 `0.6.0rc1`。
- 217 项单元/契约测试全部通过，其中包括当前 Codex CLI 的 smoke 命令兼容性检查，以及状态接口对叶子文件、父目录符号链接和多硬链接的失败关闭检查。
- 四个 Skill 校验、敏感内容扫描和 `git diff --check` 通过。
- 临时 `CODEX_HOME` 中核心模式、`inherited` 增强安装、本地验证、状态 JSON 与清除流程通过。
- `status` 在核心和增强模式下均报告 `plugin_version` 与模板版本为 `0.6.0-rc.1`，身份字段保持 `false`。
- 已从官方 npm registry 将本机 Codex CLI 修复并升级到 `0.144.6`，`codex --version` 与 `codex plugin --help` 通过。
- PR 与 main 的 Ubuntu、macOS、Windows × Python 3.11、3.12 CI 矩阵全部通过，Pages workflow 部署成功。
- 首页、support、privacy、terms 与 Discussions 均已匿名验证为 HTTP 200；Issues 与 Discussions 已启用。
- 从固定 merge SHA 的干净 checkout、隔离 `CODEX_HOME` 完成真实 marketplace 添加、安装、重复安装、Git snapshot 升级、移除，以及核心→增强→核心状态切换。

## 尚未通过

- `v0.6.0-rc.1` tag 尚未创建，其 CI 还没有运行。
- GitHub Pre-release 尚未创建，社交平台软启动尚未开始。

## 解除发布阻塞后的顺序

1. 合并本就绪记录更新，并确认最终 main CI 全绿。
2. 创建并推送 `v0.6.0-rc.1` tag，等待 tag CI 全绿。
3. 创建 GitHub Pre-release，使用对应双语草案。
4. 核验 Release 和安装入口后，开始社交平台软启动。

不得在上述门槛完成前把当前候选版描述为正式 `0.6.0`，也不得宣传固定 Token 节省比例或已经验证模型/角色身份。
