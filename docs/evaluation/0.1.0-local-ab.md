# 0.1.0 本地静态预览与 A/B 计划

日期：2026-07-11

## 摘要

Model Economy 0.1.0 已通过本地预览标签所需的静态就绪检查。验证范围包括生命周期管理、本地市场注册、插件安装、从两个无关工作目录进行静态发现，以及本地文件完整性检查。本文件是预览说明和后续 A/B 采样计划，不是已经完成的 A/B 效果报告。

本报告不声称已经证明实时任务中的模型路由效果、角色身份隔离或 token 节省幅度。这些结论需要真实的模型任务样本，仍属于后续工作。

## 静态检查

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 临时主目录生命周期 | 通过 | `install --profile inherited`、`verify`、`upgrade --dry-run`、`uninstall --purge` 与清理确认均成功完成。 |
| 本地市场注册 | 通过 | 已注册本地市场，并从 `model-economy-public` 安装插件。 |
| 真实主目录安装 | 通过 | `install --profile openai-56` 无冲突完成；重复安装为无操作。 |
| 静态诊断 | 通过 | 未使用 `--smoke`，静态诊断成功完成。 |
| 根配置保全 | 通过 | 真实主目录安装检查前后根 `config.toml` 均存在且哈希一致；未输出文件内容。 |
| 托管文件 | 通过 | 六个角色文件、Model Economy 的 `config.toml` 和 `state.json` 均存在。 |
| 跨项目发现 | 通过 | 从工作目录 A 和临时工作目录 B 执行 `plugin list --available --json`，均能发现已安装插件。 |
| 跨项目静态验证 | 通过 | 从工作目录 A 和临时工作目录 B 执行本地 `verify`，均通过。 |
| 测试套件 | 通过 | 共运行 106 项：105 项通过，1 项 Codex CLI 选项检查按设计跳过。 |
| 敏感内容扫描 | 通过 | 仓库扫描器在本地通过。 |

## 验证环境

| 项目 | 版本 |
| --- | --- |
| 操作系统 | Darwin 25.5.0 arm64 |
| Python | 3.14.4 |
| Codex CLI | 0.144.0-alpha.4 |

本机验证使用仓库 README 中的公开命令，并以逻辑工作目录 A/B 表示跨目录检查位置。报告只保留结果摘要，不公开个人路径；CI 另行覆盖 Linux、macOS、Windows 上的 Python 3.11 与 3.12。

## 跨项目验证范围

工作目录 A 是一个与本插件无关的现有目录。临时工作目录 B 是一个最小 Git 仓库，仅包含一个公开示例 Python 文件；没有从工作目录 A 向临时工作目录 B 复制文件。

跨项目检查刻意保持为静态且不调用模型。它能证明 Codex 插件列表可以发现已安装插件，也能证明从无关工作目录执行时，本地托管文件仍然有效；它不能证明新启动的模型任务一定会选择某个 Model Economy 角色。

## A/B 证据状态

| 任务类别 | 强模型调用 | 均衡/经济角色步骤 | 子代理 | 工具调用 | 耗时 | 测试 | 审查发现 | Token A/B |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 简单 | 等待真实样本 | 等待真实样本 | 等待真实样本 | 等待真实样本 | 等待真实样本 | 等待真实样本 | 等待真实样本 | 等待真实样本 |
| 标准 | 等待真实样本 | 等待真实样本 | 等待真实样本 | 等待真实样本 | 等待真实样本 | 等待真实样本 | 等待真实样本 | 等待真实样本 |
| 大型 | 等待真实样本 | 等待真实样本 | 等待真实样本 | 等待真实样本 | 等待真实样本 | 等待真实样本 | 等待真实样本 | 等待真实样本 |

首次真实 A/B 运行继续使用以下成功阈值：

- 简单任务不应调用强模型。
- 标准任务的强模型调用不应超过一次。
- 大型任务的强模型调用不应超过两次。
- 至少 70% 的实施步骤应由均衡或经济角色完成。
- 测试质量不得低于基线。

这里不估算真实 token A/B 数据。相关数据必须来自真实样本以及可信的界面或平台用量记录。

## 明确不作出的结论

- 未执行 `--smoke`。
- 本报告未人工打开或解析认证文件、会话日志或对话记录。
- 静态发现不能证明实时模型调用中的角色身份或模型身份。
- 在收集真实 A/B 样本前，不能声称已经证明 token 节省效果。
- Task 8 的本地编排资料保持忽略，不属于公开报告。

## 预览决策

当前静态检查足以支持本地 `v0.1.0` 预览标签。公开仓库发布应等待用户审阅预览并决定时机；在声称降低成本前，应先收集真实模型任务的 A/B 证据。
